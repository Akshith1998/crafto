export interface UserData {
  userName: string;
  otp: string;
}

export interface Quotes {
  id: number;
  username: string;
  text: string;
  mediaUrl: string;
  createdAt: string;
  updatedAt: string;
}

export interface MediaItem {
  url: string;
}
